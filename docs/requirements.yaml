name: docs_ZXQEP
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton
  - sphinx-autoapi



    # Pip-only installs
  - pip:
    - -e ../
    - pyzx

