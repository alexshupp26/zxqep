name: docs_ZXQEP
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - pyzx
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton




    # Pip-only installs
  - pip:
    - -e ../

